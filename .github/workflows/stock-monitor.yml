name: Bujairami stock monitor

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bujairami-stock-monitor
  cancel-in-progress: false

jobs:
  check-stock:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright/python:v1.59.0-noble
      options: --ipc=host
    env:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      STATE_FILE: stock-state.json
      PAGE_TIMEOUT_MS: "45000"

    steps:
      - name: Download monitor
        uses: actions/checkout@v4

      - name: Restore saved status
        run: |
          if [ -f .github/stock-state.json ]; then
            cp .github/stock-state.json stock-state.json
          fi

      - name: Install Python package
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Check FragranceNet
        run: python monitor.py --once

      - name: Prepare saved status
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          path = Path("stock-state.json")
          state = json.loads(path.read_text())
          keys = (
              "status",
              "product_url",
              "last_in_stock_alert",
              "last_error_alert",
              "consecutive_errors",
              "detail",
          )
          saved = {key: state.get(key) for key in keys if key in state}
          path.write_text(json.dumps(saved, indent=2, sort_keys=True) + "\n")
          PY

      - name: Pass status to save job
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stock-state-${{ github.run_id }}
          path: stock-state.json
          retention-days: 1

  save-status:
    needs: check-stock
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Download monitor
        uses: actions/checkout@v4

      - name: Download latest status
        uses: actions/download-artifact@v4
        with:
          name: stock-state-${{ github.run_id }}
          path: .github

      - name: Save status for the next check
        run: |
          git config user.name "stock-monitor[bot]"
          git config user.email "stock-monitor@users.noreply.github.com"
          git add .github/stock-state.json
          if ! git diff --cached --quiet; then
            git commit -m "Update stock monitor state"
            git push
          fi
